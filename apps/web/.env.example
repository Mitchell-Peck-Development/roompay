# Where the "Open RoomPay" button points.
NEXT_PUBLIC_APP_URL=http://localhost:3001

# The public origin of this site. Only used to make Open Graph and sitemap
# URLs absolute, so links preview correctly when shared.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
